version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - mvn package
      - echo Build completed on `date`
artifacts:
  files:
    - '*.jar'
  base-directory: 'target'
cache:
  paths:
    - '/root/.m2/**/*'

